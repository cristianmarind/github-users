interface IActions {
    add: string;
    setCurrentPage: string;
    addCount: string
}

export const ACTIONS:IActions = {
    add: "add",
    setCurrentPage: "setCurrentPage",
    addCount: "addCount"
}